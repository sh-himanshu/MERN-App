import { styled } from "@mui/system";

// we can style them later by the sx prop
export const MuiImage = styled("img")({});
export const MuiForm = styled("form")({});
export const MuiInput = styled("input")({ display: "none" });
export const MuiLabel = styled("label")({});
